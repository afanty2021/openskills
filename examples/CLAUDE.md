[根目录](../../CLAUDE.md) > **examples**

# Examples 模块

> **模块职责**: 提供 OpenSkills 技能格式示例和学习资源
> **主要语言**: Markdown
> **模块类型**: 示例和文档

## 模块职责

Examples 模块包含 OpenSkills 的技能格式示例，帮助开发者理解：
- SKILL.md 文件格式规范
- YAML 前置元数据结构
- 技能指令编写规范
- 捆绑资源的引用方式
- 最佳实践和常见模式

## 入口与启动

### 示例结构
```
examples/
├── my-first-skill/         # 入门示例
│   ├── SKILL.md           # 技能主文件
│   └── references/        # 参考文档
│       └── skill-format.md
└── CLAUDE.md              # 本文档
```

### 学习路径
1. **快速开始**: 查看 `my-first-skill/SKILL.md`
2. **深入理解**: 阅读 `references/skill-format.md`
3. **实践操作**: 尝试创建自己的技能

## 对外接口

### my-first-skill - 示例技能

**技能元数据**:
```yaml
---
name: my-first-skill
description: Example skill demonstrating Anthropic SKILL.md format
---
```

**技能功能**:
- 展示 SKILL.md 标准格式
- 说明技能创建步骤
- 演示资源引用方法
- 提供最佳实践指导

**使用场景**:
- 学习技能系统原理
- 测试 OpenSkills 加载器
- 理解 SKILL.md 格式

## 关键依赖与配置

### 技能格式规范
1. **YAML 前置元数据**
   - 必须包含 `name` 字段
   - 必须包含 `description` 字段
   - 可选包含 `context` 字段

2. **技能内容结构**
   - 使用标题组织内容
   - 命令式语态编写指令
   - 避免第二人称（"You should..."）

3. **资源引用**
   - `references/` - 文档和说明
   - `scripts/` - 可执行脚本
   - `assets/` - 模板和配置文件

### 目录约定
```
skill-name/
├── SKILL.md          # 必需：技能主文件
├── references/       # 可选：参考文档
├── scripts/          # 可选：辅助脚本
└── assets/           # 可选：资源文件
```

## 数据模型

### Skill 元数据
```yaml
---
name: string          # 技能名称（必需）
description: string   # 技能描述（必需）
context?: string      # 使用上下文（可选）
---
```

### 资源路径解析
当技能被加载时，OpenSkills 提供基础目录：
```
Base directory: /path/to/skill-name
```

相对路径解析：
- `references/doc.md` → `/path/to/skill-name/references/doc.md`
- `scripts/run.sh` → `/path/to/skill-name/scripts/run.sh`
- `assets/config.json` → `/path/to/skill-name/assets/config.json`

## 最佳实践

### 编写原则
1. **命令式语态**
   ```markdown
   ✅ 正确: "To extract text, run the extract.py script"
   ❌ 错误: "You should run the extract.py script"
   ```

2. **清晰结构**
   - 使用层级标题
   - 提供步骤编号
   - 包含代码示例

3. **资源管理**
   - 将详细文档移至 `references/`
   - 使用 `scripts/` 存放可执行代码
   - 模板文件放在 `assets/`

4. **大小控制**
   - SKILL.md 保持简洁（< 5000 字）
   - 详细内容通过资源引用

### 技能设计模式

1. **工具型技能**
   - 提供特定功能（PDF 处理、Excel 操作）
   - 包含执行脚本和依赖说明
   - 提供错误处理指导

2. **流程型技能**
   - 引导完成复杂任务
   - 分步骤说明
   - 包含检查清单

3. **模板型技能**
   - 提供代码模板
   - 包含占位符说明
   - 支持自定义配置

## 测试与质量

### 验证清单
- [ ] YAML 前置元数据格式正确
- [ ] 必需字段（name、description）完整
- [ ] 技能内容使用命令式语态
- [ ] 资源路径使用相对路径
- [ ] 指令清晰可执行

### 测试命令
```bash
# 安装并测试示例技能
openskills install ./examples
openskills list
openskills read my-first-skill
```

### 常见问题检查
- YAML 语法错误
- 缺少必需字段
- 资源路径错误
- 指令描述不清晰

## 常见问题 (FAQ)

### Q: SKILL.md 文件大小有限制吗？
A: 建议：
- 主文件保持在 5000 字以内
- 详细内容移至 references/
- 保持加载速度和可读性

### Q: 如何引用外部依赖？
A: 在技能中说明：
```markdown
## Dependencies
Install required packages:
```bash
pip install package-name
npm install package-name
```

### Q: 技能可以包含可执行代码吗？
A: 可以通过以下方式：
- 将代码放在 `scripts/` 目录
- 在 SKILL.md 中引用执行
- 提供安装和运行说明

### Q: 如何处理平台特定代码？
A: 建议做法：
- 使用跨平台语言（Python、Node.js）
- 提供多平台版本
- 明确标注支持的系统

## 扩展示例

### 复杂技能结构示例
```
advanced-skill/
├── SKILL.md
├── references/
│   ├── api-guide.md
│   ├── examples.md
│   └── troubleshooting.md
├── scripts/
│   ├── setup.py
│   ├── process.py
│   └── utils/
│       └── helpers.py
├── assets/
│   ├── template.json
│   ├── config.yaml
│   └── samples/
│       └── input.txt
└── tests/
    ├── test_skill.py
    └── fixtures/
        └── test_data.json
```

### 技能开发流程
1. **创建目录结构**
   ```bash
   mkdir my-skill
   cd my-skill
   mkdir references scripts assets
   ```

2. **编写 SKILL.md**
   - 添加 YAML 前置元数据
   - 编写技能指令
   - 引用资源文件

3. **添加资源**
   - 详细文档放入 references/
   - 可执行代码放入 scripts/
   - 模板文件放入 assets/

4. **测试技能**
   ```bash
   openskills install ./my-skill
   openskills read my-skill
   ```

5. **发布分享**
   - 推送到 GitHub
   - 编写 README
   - 添加使用示例

## 相关文件清单

### 示例文件
- `my-first-skill/SKILL.md` - 基础技能示例
- `my-first-skill/references/skill-format.md` - 格式说明

### 文档文件
- `CLAUDE.md` - 本模块文档

## 变更记录 (Changelog)

### 2025-12-07 10:21:32
- ✨ 创建 examples 模块文档
- 📚 提供完整的技能格式指南
- 💡 添加最佳实践和设计模式
- 🔧 包含开发流程和测试方法

---

*提示：examples 模块是学习和理解 OpenSkills 技能系统的最佳起点。*